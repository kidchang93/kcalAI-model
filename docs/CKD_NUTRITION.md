# 신장병(CKD) 식이 규칙 — 도메인 근거·통합 설계

> 2026-07-20 착수. 신장병 환자용 식단 추천·경고를 **대한신장학회 지침에 근거**해 강화한다.
> 코드 진입점: `services/ckd_food_rules.py` (단일 근거 모듈). 이 문서는 그 근거와 통합 설계를 설명한다.

## 0. 출처 (전부 무료 공개)

| 약칭 | 문서 | 발행 | 우리 사본 |
|---|---|---|---|
| KSN1 | 투석 전 단계의 만성콩팥병 환자를 위한 영양-식생활 관리 | 대한신장학회 | `data/1권 …관리.pdf` |
| KSN2 | 혈액투석 환자를 위한 영양-식생활 관리 | 대한신장학회 | `data/2권 …관리.pdf` |
| — | 신장질환 식품교환표(1997) · 농진청 제9개정판 표준식품성분표 | — | 칼륨 3단계 분류의 원 출처(KSN 인용) |
| — | 만성콩팥병 · 투석환자의 식이요법 | 질병관리청 국가건강정보포털 | 복막투석 보조 |
| — | KDIGO 2024 CKD Guideline | KDIGO | 국제표준(영문) |

식품별 칼륨·인 수치 원천은 우리가 이미 보유한 **농진청 국가표준식품성분표**(`import_mfds_raw.py`)·**식약처 통합식품영양성분DB**(`import_mfds_food.py`)다. 별도 라이선스·비용 없음.

## 1. 핵심 의학 사실 (지침 실측)

### 병기별 하루 목표 — `ckd_food_rules.STAGE_TARGETS`

| 영양소 | 비투석 CKD | 혈액투석 | 복막투석 |
|---|---|---|---|
| **단백질** | 0.6–0.8 g/kg (매우 저하 시 0.3–0.5) | **1.2 g/kg** | **1.2–1.3 g/kg** |
| 나트륨 | 2,000 mg 미만(소금 5g) | 3,000 mg | 3,000 mg |
| 인 | 진행 병기서 제한(~800–1,000) | 800–1,000 mg (17 mg/kg) | 800–1,000 mg |
| 칼륨 | 고칼륨혈증 시에만 | 지속 제한 | 지속 제한 |
| 열량 | 30–35 kcal/kg | 35(<60세)/30–35(≥60세) | 30–35(투석액 흡수분 감안) |

⚠️ **단백질 방향이 병기에서 뒤집힌다** — 비투석은 제한, 투석은 증량. 단일 `ckd` 조건은 이를 구분하지 못하므로, 병기별 개인 목표 산출은 **처방에 가까워** 별도 결정이 필요하다(§4).
⚠️ 칼륨·인 제한은 **진행 병기·고칼륨/고인산혈증에서** 적용된다. 초기 CKD에 과도한 제한은 오히려 영양실조를 부른다(KSN1 서문).

### 칼륨 3단계 식품 분류 (KSN1 p107–108 · KSN2)

- **채소**(두 지침 동일): 저(선택)/중(조심)/고(제한) — `VEGETABLE_K_LOW/MID/HIGH`.
  - 고칼륨(제한): 아욱·근대·머위·미나리·부추·쑥·시금치·죽순·취·미역(생)·단호박·늙은호박·쑥갓·고춧잎.
- **과일**: 비투석(KSN1)은 관대, 혈액투석(KSN2)은 엄격(귤·포도가 저→중). 기본은 **투석 기준(엄격)** 인코딩, `on_dialysis=False`면 완화.
  - 고칼륨(제한): 곶감·멜론·바나나·앵두·참외·천도복숭아·토마토·방울토마토·키위.

### 고인 식품 (제한) — `HIGH_PHOSPHORUS_KEYWORDS`

유제품·견과류·잡곡/현미·탄산음료·초콜릿·가공식품(햄/소시지/어묵)·뼈째 생선(멸치/뱅어포). **살코기·생선·달걀 같은 필수 단백질원은 제외**(끊으면 안 되고 인결합제로 관리 — KSN2 p141).

### 고나트륨 식품 (제한) — `HIGH_SODIUM_KEYWORDS`

젓갈·장아찌·자반·라면·어묵·햄·소시지·단무지·절임·장조림·명란·김치류. (국·장류는 한식 주류라 과잉 경고 대신 **저염 조리** 안내로 다룬다.)

### 칼륨 저감 조리법 — `POTASSIUM_REDUCTION_TIPS`

껍질·줄기 제거 → 얇게 썰어 10배 물에 2시간 담금 → 데친 뒤 여러 번 헹굼 / 잡곡밥→흰쌀밥 / 말린 과일 회피(신선·통조림 시럽 제외).

## 2. 왜 이게 필요했나 — 데이터 커버리지 실측 (2026-07-20)

식약처 음식(요리) DB `source='mfds'` 추천 후보 풀의 칼륨·인 커버리지(고유명 dedupe):

- **식사 대분류(아침/점심/저녁)**: 합계 2,197행 중 K/P 있음 1,823행 = **83%**. 국·탕·찌개·나물·찜·전·김치류 100%, 볶음 93%, 밥 92%. → 수치 정렬이 **식사에선 이미 잘 동작**.
- **간식 대분류**: 빵·과자 1%, 음료·차 1% → 칼륨·인 사실상 없음 → CKD 간식 정렬이 NULL 위 랜덤.
- 전체 평균이 "22%"로 낮게 보이는 건 빵(8,032행)·음료(4,638행)가 행 수를 압도하는 착시.

**결론**: 수치 정렬은 **식사에 유효**하고, 간식은 **이름 기반 지침 분류**로 메운다(상보적). 그리고 어느 쪽이든 사용자에게 **수치를 보여준 적이 없다** → "정보를 안 쓴다"는 체감의 진짜 원인.

## 3. 통합 설계 (이번 구현 범위 — 비처방)

### 3-1. 추천에 실측 영양 노출 + 지침 근거 (`recommendation_*`)

`GET /api/recommendations` 계약 변경 (전부 **추가·nullable → 하위호환**, 서버·앱 동시 배포):

- `RecommendationItem` += `sodium_mg` · `potassium_mg` · `phosphorus_mg` · `protein_g` (`float | None`) — DB 실측값 그대로. 원물·미측정 행은 None.
- `RecommendationResponse` += `tips: list[str]` — 사용자 조건 기반 안내(CKD면 칼륨 저감 조리법). 비CKD면 빈 배열.
- 앱은 카드에 수치 칩을, 하단에 tips를 렌더. 값 없으면 숨김.

### 3-2. 고칼륨/고인 후보 배제 (후보 풀) — 이름 + 실측 이중

`_candidate_pool`에서 `low_potassium`/`low_phosphorus` 태그가 있으면 두 축으로 고칼륨·고인 후보를 **하드 배제**한다:

1. **이름 기반**(`POTASSIUM_HIGH_KEYWORDS`·`HIGH_PHOSPHORUS_KEYWORDS` NOT LIKE) — 수치가 없는 간식(바나나·초콜릿·견과)을 잡는다.
2. **실측 상한**(`potassium_mg <= 600` · `phosphorus_mg <= 400`, NULL은 통과) — 정렬만으론 성긴 그룹에서 고칼륨이 새어든다(2026-07-20 로컬 검증: 고구마 804 mg·돼지 간 409 mg 누수 발견 → 상한 도입으로 0건). NULL은 이름 기반이 받는다.

**왜 정렬이 아니라 배제인가**: 고칼륨혈증은 급성 심정지 위험(KSN2)이라 "낮은 순 정렬 후 상위 절반에서 추첨"으론 부족하다. 칼륨(600)은 엄격, 인(400)은 관대 — 인은 만성이고 필수 단백질원을 지켜야 한다. 두 상한은 지침의 절대 컷오프가 아니라 **정책값**이다(§4). 전분질 곡류(고구마·감자·토란)는 KSN 채소/과일 분류 밖이라 `STARCHY_K_HIGH`로 이름 목록에 보강했다.

### 3-3. 기록 경고를 지침 근거로 강화 (`get_record_warnings`) — 구현됨

영양 제한 태그(low_sodium/low_potassium/low_phosphorus)가 있는 질병(**신장병·고혈압**)은 기존 키워드 매칭 대신 `ckd_food_rules`의 칼륨/인/나트륨 분류로 판정하고, 경고 항목에 **어느 영양소인지**(`nutrient`)를 담아 "'시금치나물'은 칼륨이 높은 편이에요"로 안내한다. 그 외 질병(당뇨·임신·암)은 기존 exclude_keywords 경로 유지(nutrient=None).

- 계약: `NutritionWarningItem` · 앱 `FoodWarning` += `nutrient: sodium|potassium|phosphorus | null`(추가·nullable → 하위호환). 앱 `formatWarning`이 nutrient 있으면 축 문구로 분기.
- CKD 기존 3키워드(젓갈·장아찌·라면)는 `HIGH_SODIUM_KEYWORDS`에 흡수돼 손실 없이 확장(시금치·바나나·고구마=칼륨, 치즈·아몬드=인 등). 고혈압은 나트륨 축만.
- dedupe 키에 nutrient 포함 — 한 음식이 두 축에 걸리면 각각 알린다.

### 3-4. 수치의 상대 등급 UI 노출 (`potassium_tier`·`phosphorus_tier`) — 2026-07-21

"칼륨 320 mg"만 띄우면 환자는 그게 높은지 낮은지 모른다. 카드에 **저/중/고**를 붙여 상대 위치를 보인다.

- 계약(추가·nullable → 하위호환, 서버·앱 동시 배포): `RecommendationItem` += `potassium_tier`·`phosphorus_tier`(`"low"|"mid"|"high"|null`), `RecommendationResponse` += `tier_notice: str|None`.
- **칼륨·인 제한 태그를 가진 사용자에게만** 채운다. 비CKD에게 칼륨 등급은 노이즈다. 나트륨·단백질은 1인분 기준 근거가 병기에 따라 갈려 **등급을 매기지 않는다**(수치만).
- **저장하지 않고 매 요청 계산**한다(`_annotate_tiers`). 질병을 추가·삭제하면 캐시된 등급이 거짓이 되기 때문이며, `tips`와 같은 정책이다.
- 판정은 **지침 이름 분류와 실측 mg 등급 중 엄격한 쪽**(`_stricter`)이다. 근거가 둘 다 없으면 `null` → 앱이 배지를 숨긴다.
- 인은 이름 근거를 실측보다 약하게 두지 않는다 — 가공식품·유제품의 인은 흡수율 높은 **무기인**이라 같은 mg이라도 부담이 크다(KSN2 p141).

| 축 | mid 경계 | high 경계 | 근거 |
|---|---:|---:|---|
| 칼륨 | 200 mg | 400 mg | 신장질환 식품교환표 고칼륨 >200 mg/교환단위를 1교환=mid, 2교환=high로 |
| 인 | 150 mg | 300 mg | 하루 800~1,000 mg(KSN2) ÷ 3끼 ≈ 270~330을 high로, 그 절반을 mid로 |

⚠️ 두 경계 모두 **정책값**이다(§4의 "절대 임계값 부재"와 같은 지위). 그래서 등급을 노출할 때는 반드시 `TIER_NOTICE`를 함께 내려보낸다 — 상대 안내이며 목표량은 병기·검사에 따라 다르니 의료진과 상담하라는 고지다. 표시 문구는 **낮음/보통/높음**이며 "위험·금지"로 읽히는 표현을 쓰지 않는다.

**실측 관찰(2026-07-21 로컬)**: CKD 사용자의 추천 카드는 칼륨 오름차순 정렬 + 상한 하드 배제(§3-2)를 이미 통과한 결과라 **거의 항상 `low`** 로 나온다. 등급은 이 화면에서 "안심하고 골라도 된다"는 신호로 기능하고, `mid`/`high`가 실제로 의미를 갖는 곳은 **사용자가 먹은 음식**(기록·끼니 구성)이다.

### 3-5. 먹은 음식의 수치 — estimate 노출 + 경고의 실측 축 — 2026-07-21

추천(3-1·3-4)은 **먹기 전** 화면이라 이미 걸러진 음식만 나온다. 정작 환자가 알아야 할 건 **자기가 먹은 음식**의 수치인데 거기엔 아무 정보가 없었다.

**(a) `POST /api/nutrition/estimate` += `sodium_mg`·`potassium_mg`·`phosphorus_mg`** (nullable, DB 실측 그대로)

이 라우트는 **질병을 읽지 않으므로 동의 요건이 그대로다**(Bearer만). 수치는 음식의 사실이지 사용자의 건강정보가 아니다. 등급(tier)을 여기에 실으면 질병 태그를 읽어야 해서 `sensitive_health` 동의가 필요해지고, 미동의 사용자의 칼로리 조회까지 403이 된다 — 그래서 **등급은 경고 API가 담당**한다.

앱은 이 값을 초안에 싣고 **선택한 양(`serving_ratio`)을 곱해** 보여준다(kcal과 같은 규칙). 등급은 음식 자체의 성질이라 1인분 기준 판정을 그대로 쓴다 — 양을 줄여도 고칼륨 식품은 고칼륨이다.

**(b) `POST /api/nutrition/warnings` — 실측을 두 번째 판정 축으로 + 근거 수치 노출**

기존 경고는 **이름 기반 지침 키워드만** 봤다. 그 목록은 원물 중심(시금치·바나나)이라 **요리명이 통째로 새어 나갔다**. 실측으로 확인한 누수:

| 음식 | 이름 매칭 | 실측 칼륨 | 기존 | 지금 |
|---|---|---:|---|---|
| 닭찜_안동찜닭 | 없음 | 3,120 mg | 경고 없음 | 칼륨·인 경고 |
| 칼국수_간편조리세트_닭한마리와 칼국수 | 없음 | 3,507 mg | 경고 없음 | 칼륨 경고 |

혈액투석 환자의 하루 칼륨 목표를 **한 끼로 넘기는** 음식들이 조용히 통과하고 있었다. 그래서 추천의 이름+실측 이중 방어(3-2)를 경고에도 대칭으로 적용한다 — **이름에 걸리거나 실측 등급이 `high`면 알린다.**

- 계약: `NutritionWarningItem` += `nutrient_mg: float|None`·`tier: "low"|"mid"|"high"|None`. 앱이 "칼륨이 높은 편이에요 **(1인분 681mg · 높음)**"처럼 근거를 함께 보인다 — 수치 없이 '높은 편'만 말하면 사용자가 판단할 근거가 없다.
- 실측만으로 발동한 경고는 걸린 키워드가 없어 `matched_keyword`가 **빈 문자열**이다.
- 실측 조회는 **정확·공백무시 일치만** 쓴다(`_measured_for_warning`). estimate와 달리 유사도(trgm) 매칭을 쓰지 않는다 — 이름이 비슷한 다른 음식의 칼륨으로 "높다"고 알리면 틀린 경고가 되고, 경고는 한 번 틀리면 전부 무시된다.
- 나트륨은 등급을 매기지 않으므로(3-4) 실측 발동도 없다 — 기존 이름 기반 그대로다.

### 노출 원칙 (책임)

- **보여주는 것**: 실측 수치 + 지침의 상대 분류(저/중/고) + 저감 조리법 + 고지문. → 사실·상대 안내, **처방 아님**.
- 기존 고지문 유지: `"AI 추정값이며 의학적 조언이 아닙니다."` (CKD 화면엔 "제한 여부·목표량은 병기·검사에 따라 다르니 의료진·영양사와 상담하세요" 보강 권장).

## 4. 보류 — 결정 필요 (처방 근접)

- **병기(비투석/혈액투석/복막투석) 구분 + 개인 단백질 목표(g/kg) 산출.** 단백질 방향 반전을 올바로 반영하려면 필요하나, 개인 목표량 제시는 처방에 근접한다. 데이터모델(건강 프로필에 병기 필드, 비암호화 — 기존 condition/allergen 관례) + 온보딩 + 앱 변경이 따른다. `STAGE_TARGETS`·`PROTEIN_DIRECTION_NOTE`에 근거는 준비돼 있다.
- **절대 임계값**("칼륨 200mg 이상 위험"): 공식 절대 컷오프 부재. 도입 시 신장질환 식품교환표(200mg/교환단위) 근거 명시 + 고지문 강화 필요.
- **추천 배제 상한값**(`POTASSIUM_SERVING_HIGH_MG=600`·`PHOSPHORUS_SERVING_HIGH_MG=400`)은 교환표 200mg/교환단위를 한 끼 제공량으로 환산한 **보수적 정책값**이다. 지침의 공식 컷오프가 아니므로 임상 감수 시 조정한다. 현재는 명백한 고칼륨/고인(고구마·간)을 거르는 안전장치로만 쓰고, 사용자에겐 "위험/금지"가 아니라 추천 후보 선정 기준으로만 반영한다.
